import { FormEvent, useState } from 'react';
import { FiArrowRight, FiBox, FiCheckCircle, FiPlus, FiTruck } from 'react-icons/fi';

interface Transfer { id: string; product: string; route: string; quantity: number; status: 'In transit' | 'Received' | 'Draft'; date: string }
const startingTransfers: Transfer[] = [
  { id: 'ST-2026-018', product: 'Wireless Mouse', route: 'Main Warehouse → Patna Branch', quantity: 36, status: 'In transit', date: '16 Sep 2026' },
  { id: 'ST-2026-017', product: 'Mechanical Keyboard', route: 'Main Warehouse → Gaya Branch', quantity: 18, status: 'Received', date: '14 Sep 2026' },
  { id: 'ST-2026-016', product: 'USB Hub', route: 'Patna Branch → Main Warehouse', quantity: 12, status: 'Draft', date: '13 Sep 2026' },
];

export function StockTransfersPage() {
  const [transfers, setTransfers] = useState(startingTransfers); const [open, setOpen] = useState(false); const [product, setProduct] = useState(''); const [quantity, setQuantity] = useState(''); const [from, setFrom] = useState('Main Warehouse'); const [to, setTo] = useState('Patna Branch');
  const createTransfer = (event: FormEvent) => { event.preventDefault(); if (!product.trim() || !Number(quantity)) return; setTransfers((current) => [{ id: `ST-2026-${String(19 + current.length).padStart(3, '0')}`, product, route: `${from} → ${to}`, quantity: Number(quantity), status: 'Draft', date: '16 Sep 2026' }, ...current]); setProduct(''); setQuantity(''); setOpen(false); };
  const receive = (id: string) => setTransfers((current) => current.map((item) => item.id === id ? { ...item, status: 'Received' } : item));
  return <div className="space-y-6"><section className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.22em] text-indigo-600">Inventory · Movement</p><h1 className="mt-2 text-3xl font-semibold text-slate-950">Stock transfers</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-500">Move inventory between warehouses and branches with a clear receiving trail.</p></div><button onClick={() => setOpen(true)} className="inline-flex items-center justify-center gap-2 rounded-xl bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white hover:bg-indigo-500"><FiPlus /> New transfer</button></div></section><section className="grid gap-4 md:grid-cols-3"><Summary label="In transit" value={String(transfers.filter((item) => item.status === 'In transit').length)} icon={<FiTruck />} /><Summary label="Awaiting receipt" value={String(transfers.filter((item) => item.status === 'Draft').length)} icon={<FiBox />} /><Summary label="Received this month" value={String(transfers.filter((item) => item.status === 'Received').length)} icon={<FiCheckCircle />} /></section><section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-100 p-4"><h2 className="font-semibold text-slate-900">Transfer register</h2></div><div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-3">Transfer</th><th className="px-5 py-3">Product</th><th className="px-5 py-3">Route</th><th className="px-5 py-3">Qty</th><th className="px-5 py-3">Status</th><th className="px-5 py-3">Action</th></tr></thead><tbody className="divide-y divide-slate-100">{transfers.map((transfer) => <tr key={transfer.id}><td className="px-5 py-4 font-semibold text-slate-900">{transfer.id}<p className="mt-0.5 text-xs font-normal text-slate-400">{transfer.date}</p></td><td className="px-5 py-4 text-slate-600">{transfer.product}</td><td className="px-5 py-4 text-slate-600">{transfer.route}</td><td className="px-5 py-4 font-medium text-slate-900">{transfer.quantity}</td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-medium ${transfer.status === 'Received' ? 'bg-emerald-50 text-emerald-700' : transfer.status === 'In transit' ? 'bg-amber-50 text-amber-700' : 'bg-slate-100 text-slate-600'}`}>{transfer.status}</span></td><td className="px-5 py-4">{transfer.status === 'In transit' ? <button onClick={() => receive(transfer.id)} className="text-sm font-medium text-indigo-600 hover:text-indigo-500">Mark received</button> : <span className="text-slate-400">—</span>}</td></tr>)}</tbody></table></div></section>{open && <div className="fixed inset-0 z-[70] grid place-items-center bg-slate-950/50 p-4"><form onSubmit={createTransfer} className="w-full max-w-lg rounded-3xl bg-white p-6 shadow-2xl"><h2 className="text-lg font-semibold text-slate-900">Create stock transfer</h2><p className="mt-1 text-sm text-slate-500">This draft is stored in the frontend only.</p><div className="mt-5 grid gap-4 sm:grid-cols-2"><Field label="From"><select value={from} onChange={(e) => setFrom(e.target.value)}><option>Main Warehouse</option><option>Patna Branch</option><option>Gaya Branch</option></select></Field><Field label="To"><select value={to} onChange={(e) => setTo(e.target.value)}><option>Patna Branch</option><option>Main Warehouse</option><option>Gaya Branch</option></select></Field></div><div className="mt-4"><Field label="Product"><input autoFocus value={product} onChange={(e) => setProduct(e.target.value)} placeholder="Product name or SKU" /></Field></div><div className="mt-4"><Field label="Quantity"><input type="number" min="1" value={quantity} onChange={(e) => setQuantity(e.target.value)} placeholder="0" /></Field></div><div className="mt-6 flex justify-end gap-2"><button type="button" onClick={() => setOpen(false)} className="rounded-xl px-4 py-2 text-sm text-slate-600 hover:bg-slate-100">Cancel</button><button className="rounded-xl bg-indigo-600 px-4 py-2 text-sm font-semibold text-white hover:bg-indigo-500">Create draft</button></div></form></div>}</div>;
}
function Summary({ label, value, icon }: { label: string; value: string; icon: React.ReactNode }) { return <div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between text-slate-500"><span className="text-sm">{label}</span><span>{icon}</span></div><p className="mt-2 text-2xl font-bold text-slate-900">{value}</p></div>; }
function Field({ label, children }: { label: string; children: React.ReactNode }) { return <label className="block text-sm font-medium text-slate-700">{label}<span className="mt-1.5 block [&>input]:h-10 [&>input]:w-full [&>input]:rounded-xl [&>input]:border [&>input]:border-slate-200 [&>input]:px-3 [&>select]:h-10 [&>select]:w-full [&>select]:rounded-xl [&>select]:border [&>select]:border-slate-200 [&>select]:px-3">{children}</span></label>; }
